# Strict Project Rules

* NEVER create a new project
* ONLY modify existing files
* NEVER overwrite the whole codebase
* Preserve working UI
* Keep code beginner-friendly
* Keep JSX readable
* Keep Tailwind classes simple and editable
* Avoid over-engineering
* Do not create unnecessary folders/files
* Preserve imports/routes unless fixing them
* Do not remove working sections unless explicitly instructed
